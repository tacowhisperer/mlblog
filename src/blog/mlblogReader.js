/**
 * @author tacowhisperer
 */

const mlblog = require('mlblog').mlblogFactory;

/**
 * Controller for the Mexico Lindo Blog Reader. It reads the data stored on the Google
 * Spreadsheets workbook, and outputs that data to a Promise that either resolves or rejects.
 * @param {string} sheetId The ID of the Google Spreadsheet that holds the database information.
 */
function mlblogReader(sheetId) {
	const ID = sheetId;


}