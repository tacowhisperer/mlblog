/**
 * @author tacowhisperer
 */

/**
 * Top level tokens defined in all submodules that inherit from the Component class.
 */
const TOKENS = {
	COMPONENT: /\{\{.*?\}\}/gi
};

/**
 * Top level class that encompasses the Component.js framework for dynamically building HTML views without
 * the need to explicitly hard-code everything into an HTML file.
 * @param {string} markup The HTML-like markup string that should be compiled into a workable HTML object.
 */
function Component(markup) {

}
